name: Tests

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  test:
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            node-version: 20.x
            python-version: "3.10"
          - os: ubuntu-latest
            node-version: 22.x
            python-version: "3.12"
          - os: macos-latest
            node-version: 22.x
            python-version: "3.12"
          - os: windows-latest
            node-version: 22.x
            python-version: "3.12"

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}

      - name: Setup Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: npm ci

      - name: Run JavaScript and Python tests
        run: npm test

      - name: Check build output
        run: node -e "const { existsSync } = require('node:fs'); if (!existsSync('dist')) throw new Error('Build failed - dist directory not found');"

      - name: Verify package exports and artifacts
        run: npm run verify:package

      - name: Verify benchmark sources
        run: npm run verify:benchmarks

      - name: Verify packed artifact in an empty consumer
        run: npm run verify:packed

  packed-browser:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22.x

      - name: Setup Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: npm ci

      - name: Install Chromium
        run: npx playwright install --with-deps chromium

      - name: Verify packed artifact in Chromium
        run: npm run verify:browser
